# 改造进度与会话交接

## 1. 当前摘要

| 项目 | 当前值 |
| --- | --- |
| 当前阶段 | 阶段 1、阶段 2、阶段 3、阶段 5 已完成；进入正式收口前复核 |
| 当前分支 | `refactor/local-ai-workflow-redesign` |
| 基线 | `b13a70d53d885c8f9dd93c3de28df16a53e7a380` |
| 当前目标 | 固化从初始来源、可选 Spec、代码、PR 到 Issue 收尾的单向开发流 |
| 当前状态 | 层次边界、重大变化条件、PR 差异说明和一次性 Issue 交付评论规则已落地、通过验证并经用户确认 |
| 最近更新 | 2026-08-04 |
| 下一步 | 进入阶段 6 正式收口，复核完整改造并删除本临时目录及项目入口提示 |

## 2. 已完成

- 已完整阅读两篇用户提供的工作流优化文档；
- 已核对当前项目入口、Skill 索引、三条交付路径、Spec 状态脚本、工具测试、文档同步规则和 GitHub Actions；
- 已确认详细来源文档中的 Brief → Acceptance → Technical Design 现状已经过时；
- 已确认当前仍存在入口偏重、本地恢复范围有限、续改入口不自然、证据按全仓失效等问题；
- 已从最新 `origin/dev` 创建独立分支 `refactor/local-ai-workflow-redesign`；
- 已建立 `plans/ai-workflow-redesign/`，并将分析、计划、决定和进度拆分保存；
- 已在项目入口增加一条仅对本改造分支生效的临时读取指针；
- 曾形成三种工作方式、两档风险、按需产物、人工职责和四个完成边界的候选；其中工作方式主分类已被七维判断取代，续改和完成边界仍待确认；
- 已形成跨会话记录的候选方案，区分当前有效内容、按需支持材料、临时运行信息和最终长期事实；
- 已根据技术依赖调整顺序：第一批先做最小 Change 创建与恢复，第二批再做检查点和 `amend`；
- 已进一步复核复杂度并暂停上述第一批候选：它会新增第二套任务身份、状态和命令，不符合当前首要目标；
- 已核对 Codex 官方 worktree 说明和仓库现有配置，确认 `.worktreeinclude` 可以在新建本地托管 worktree 时复制匹配的 Git 忽略文件；同时确认它不是持续同步，也不覆盖远程或命令行 worktree；
- 已把 `.specs + .worktreeinclude` 确认为本机跨工作树恢复方式，并修改正式 `.worktreeinclude`；
- 已由用户确认七维判断：准备程度；影响面与相互牵连；规则与状态关系；实现确定程度；验证难度；风险与可回退性；持续性与协作；
- 已确认只有中间四项决定复杂度，不打数字总分；准备是开工门，风险和记录需要独立判断；
- 已修改正式 `flow-router`、项目入口、直接实施消费规则和相关 Skill 说明，允许符合条件的中等会话级任务直接实施，严格风险不再单独触发方案先行；
- 已增加最小语义契约检查，要求分流输出准备、复杂度、风险、记录和路径，并拒绝旧“一项不满足就方案先行”规则；
- 已运行两项新增目标回归测试和 Skill 校验，结果通过；
- 已运行 `npm run check:ai`，项目链接、19 个 Skill、长期文档同步规则和 14 组运行时契约全部通过；
- 已运行完整工作流工具测试文件，全部通过；
- 独立审查发现并推动修正了直接实现仍依赖冻结方案、模块规划顺序、单需求问题误转飞书、契约守卫转交和项目总入口回流五类冲突；复审确认没有剩余阻断或重要规则冲突；
- 已给分流核心语义和两个下游消费 Skill 增加删除变异测试，防止只保留输出标签却恢复旧行为；
- 最终完整 `npm run check` 通过；
- 已用“简历只读分享链接”模拟精简后的 `solution.md`；用户审阅后确认主要流程图仍须保留，并确认业务流程作为核心、数据模型按实际影响分层；
- 曾把方案模板从固定 13 节收敛为 6 个核心章节和 1 个按需收口章节；用户对比后于 2026-08-02 调整决定，现已恢复原有 13 章作为完整章节库，并保留独立“验证与验收”；
- 已把需求描述设为固定核心，把状态机和数据模型设为高优先条件章节，同时保持其他章节按需求挑选；
- 已把目录树、文件职责、模块实现和实施顺序合并为一张代码施工表，并用 `R/BR → 实施步骤 → 验证项` 保证可指导代码生成；
- 已重写 `solution-generator` 的组装、提问、数据分层、施工和冻结规则，明确未命中的按需章节直接删除；
- 已同步项目入口、Spec 说明、Acceptance、实施、测试、人工验收、质量审查和完成度审计，不再依赖固定方案节号；
- 已修正人工验收模板对 `acceptance.feature` 的固定依赖，使直接施工路径可以引用 `solution.md`；
- 已把方案模板机器检查改为完整章节库、重点条件章节、施工契约和按需选择能力检查，并增加缺失需求描述、状态机、数据模型、代码计划、验证映射及按需规则的回归测试；
- 已把固定方案节号的防回退检查扩到全部项目 Skill，并在 Acceptance 模板中示范 `R/BR` 来源注释；
- 已运行 `check_skills.py`、完整工作流工具测试、`npm run check:ai` 和最终 `npm run check`，结果通过；
- 独立只读复核未发现阻断或重要问题，确认按需删除章节不会放松数据、权限、契约、兼容、发布和回退要求。
- 已按 D-028 重新运行目标检查、完整 `npm run check` 和独立复核；确认 13 个原章节、第 14 章验证映射、状态与数据重点规则及代码生成链路全部有效。
- 已让旧状态升级时同步迁移真实 `brief.md`，并兼容此前已错误升级到 schema v7 的遗留状态；旧 schema 或 v7 下两个文件内容不同时都停止且不覆盖。
- 已禁止在冻结 Acceptance 时使用 `--next` 绕过显式路径切换，失败时保持 route、phase 和冻结状态不变。
- 已让每次重新验证在执行前撤销旧验证和质量审查；失败命令保留真实退出码，但任务回到 `implementation`，不再继续显示 `release_ready`。
- 已补充旧文件真实迁移、schema v7 遗留修复、旧 schema 与 v7 双文件冲突、非法 `--next`、命令失败重验和冻结产物漂移重验的回归测试；完整工作流工具测试 43 项通过。
- 独立审查发现并推动补全了 v7 双文件冲突和冻结产物漂移重验两个边界；复审确认没有剩余的可信状态漏洞。
- 已把用户当前请求、可读取 Issue、飞书文档和用户指定材料统一为有效来源；Issue 只作可选追踪，无 Issue 使用本地任务标识，不阻止 Spec、实现、分支或 PR。
- 已让 `flow-router` 继续在内部完整判断七维，但普通任务默认只展示路径、一个主导原因和额外检查；准备不足、严格风险、持久记录或用户主动询问时才展开。
- 已让短且无真实待决选择的方案整份只确认一次；用户已选后续路径时冻结阶段直接复用，模块规划读回后也复用原请求的继续授权和指定路径。
- 已把 `implementation_report.md` 缩成已允许方案偏差、已接受限制、跨会话遗留风险与接手点三类增量章节；数据库、跨端、权限、部署、多模块和严格风险本身不再触发。
- 已把任务范围验证与 PR 全量验证分离：Spec 可绑定一条或多条范围命令，`release_ready` 只代表任务范围验证和质量审查；PR 前仍必须运行完整 `npm run check`，共享 CI 保持完整，迁移保留专项链检查。
- 已同步项目入口、Skill 索引、Solution、Acceptance、实施、人工验收、质量审查、完成度审计、文档同步、迁移、Git 与 Spec 说明，并用删除变异测试保护五项减法不被后续文案改回。
- 已按用户最终决定完整删除 `scripts/spec/flow_guard.py` 和 `package.json` 中的 `npm run spec` 入口，不保留旧任务迁移或兼容读取。
- 已删除方案、Acceptance、实现、验证、审查和发布之间的机器阶段、冻结、哈希和验证快照；没有增加状态替代文件或整份方案确认总开关。
- 已把方案任务的恢复和执行统一为：读取当前 `solution.md` 和实际存在的附件，再结合当前请求、Git 差异、真实代码和实际验证判断下一步。
- 已明确直接实现可以跨会话继续，不因为持久记录自动升级方案，也不要求补建 Spec 或实施报告。
- 已把 `.specs/*/` 加入 `.worktreeinclude`，并在正式规则中说明它只在新建本地 Codex 托管工作树时复制一次，不是持续同步。
- 已增加无状态工作流契约和删除变异测试，覆盖旧状态脚本、任意 `scripts/spec/` 脚本引用、npm 状态入口、状态引用、换皮状态字段、Git 忽略和工作树复制规则的防回退。
- 已完成一次无状态新会话前向模拟：在没有 `state.yaml` 的情况下可从当前方案、Git 和代码恢复到实现、验证、审查与收尾；只有真实业务未决项、多份 Spec 无法辨认、人工环境或 Git 授权仍需用户介入。
- 已按“没有旧任务”的确认清理主检出目录和两个旧 Codex 工作树中的三份旧本地 Spec；为保留误删恢复能力，它们已移动到 macOS 废纸篓目录 `LinkCV-old-specs-20260802-stateless-cleanup`。再次扫描主检出和全部 Codex 工作树，没有剩余 `state.yaml` 或 `state.yml`。
- 已确认并落地飞书与 Spec 的单向关系：飞书只作为方案形成前的初步设计输入；`solution.md` 确认后成为方案任务的实施与验收依据；后续冲突只修订原方案，不回写飞书。
- 已同步 Skill 索引、方案生成、验收契约、实施回流、契约分析、完成度审计、模块规划、分流入口和 `.specs/README.md`，并增加语义检查与删除变异测试防止恢复“飞书优先”或“冲突后回写飞书”。
- 已把飞书、Issue 正文、Spec、代码与测试、PR、Issue 交付评论划分为初始设计、任务跟踪、可执行方案、实施真相、交付审阅和跟踪收尾六个层次；上游不做强一致或反向同步。
- 已规定普通实现调整只改代码并按需写 PR，同一目标的重要决定只修订当前 `solution.md`，只有原目标或核心验收失效、形成独立交付边界或模块前提重建时才开启新一轮。
- 已给 PR 增加“来源差异与取舍”，给来源 Issue 增加一次性交付评论模板；创建 PR 的授权默认包含该评论，但不修改 Issue 正文、状态或其他字段。

## 3. 当前停点

阶段 1、阶段 2 和无状态阶段均已完成并提交。本次项目级文档减法把 `.ai/prompts/project.md` 从 158 行压到约 85 行，只保留开工入口、通用底线和用户放行方式；原简历编辑器与管理端规则经代码核实后迁入 `docs/internals/web.md`。用户已审阅并要求提交。原 `phase-1-plan.md` 继续作为已否决 Change 双轨方向的历史材料，不能作为施工依据。

## 4. 本次不做或尚未处理

- 不再计划实施 `amend`、检查点历史、派生状态或验证证据缓存；这些方向已由 D-034 和 D-036 取消；
- 尚未修改任何 Issue、飞书或其他外部系统；
- 七维分流、Solution 改造、五项减法和无状态改造均已提交；项目入口精简和 Web 约束迁移纳入本次提交，整个分支尚未推送或创建 PR。

## 5. 验证记录

| 时间 | 命令 | 结果 | 说明 |
| --- | --- | --- | --- |
| 2026-08-01 | `git status --short --branch` | 通过 | 建立施工目录前工作区干净，位于预期改造分支 |
| 2026-08-01 | `git diff --check` | 通过 | 临时入口和施工文档没有空白错误 |
| 2026-08-01 | `npm run check:ai` | 通过 | 链接、19 个项目 Skill、文档同步规则和 14 组运行时契约均通过 |
| 2026-08-01（候选模型后） | `git diff --check` 与未跟踪 Markdown 的同等检查 | 通过 | 新增和修改的候选模型、决定、计划与交接记录没有空白错误 |
| 2026-08-01（候选模型后） | `npm run check:ai` | 通过 | 已检查 7 个变更文件；链接、19 个项目 Skill、7 条文档同步规则和 14 组运行时契约均通过 |
| 2026-08-01（跨会话方案后） | 空白检查与 `npm run check:ai` | 通过 | 已检查 8 个变更文件；跨会话候选文档未破坏项目链接、Skill、文档同步规则或运行时契约 |
| 2026-08-01（第一批计划后） | 空白检查与 `npm run check:ai` | 通过 | 已检查 9 个变更文件；阶段顺序、详细计划和项目现行约束保持一致 |
| 2026-08-01（复杂度复核后） | 空白检查与 `npm run check:ai` | 通过 | 已检查 9 个变更文件；已暂停双系统候选并把“先给现有 Spec 做减法”写入计划，未改变现行行为 |
| 2026-08-01（`.worktreeinclude` 方案后） | 空白检查与 `npm run check:ai` | 通过 | 已检查 9 个变更文件；本机复制候选已写入分析和计划，正式 `.worktreeinclude` 与现行工作流行为均未修改 |
| 2026-08-01（七维分流实现中） | `pytest ... -k 'flow_router_contract or legacy_flow_router_rule'` | 通过 | 两项新增测试通过：必要输出字段受保护，旧一票升级规则会被拒绝 |
| 2026-08-01（七维分流实现中） | `python scripts/quality/check_skills.py` | 通过 | 19 个项目 Skill 及新的七维分流契约通过校验 |
| 2026-08-01（七维分流实现后） | `pytest tests/tooling/test_ai_workflow.py -q` | 通过 | 完整工作流工具测试文件通过，包括新增的七维契约和旧规则拒绝测试 |
| 2026-08-01（七维分流实现后） | `npm run check:ai` | 通过 | 项目链接、19 个 Skill、长期文档同步规则和 14 组运行时契约通过 |
| 2026-08-01（七维分流实现后） | `npm run check` | 环境阻塞 | 首次运行在 Web 类型检查时报 `tsc: command not found`，当前 worktree 尚未安装前端依赖；此前 AI 工作流检查已通过 |
| 2026-08-01（七维分流实现后） | `npm run setup` | 通过 | 按仓库统一入口安装既有依赖并校验 AI 安全链接，没有改动受 Git 管理的依赖文件 |
| 2026-08-01（七维分流实现后） | `npm run check` | 通过 | Web/扩展类型检查与三端构建通过；Web 99 项、扩展 15 项、后端 191 项测试通过，后端 4 项跳过并有 1 条第三方弃用警告 |
| 2026-08-01（独立复核修正后） | 七维语义删除变异测试 | 通过 | 4 项目标测试通过；核心分流、直接实现消费依据和 `contract-guard` 转交规则均受校验保护 |
| 2026-08-01（阶段 1 最终） | 独立只读复核 | 通过 | 未发现剩余阻断或重要规则冲突；确认 Spec schema、route 和状态机未改 |
| 2026-08-01（阶段 1 最终） | `npm run check` | 通过 | Web/扩展类型检查与三端构建通过；Web 99 项、扩展 15 项、后端 193 项测试通过，后端 4 项跳过并有 1 条第三方弃用警告 |
| 2026-08-01（方案模板精简后） | `check_skills.py` 与 `pytest tests/tooling/test_ai_workflow.py -q` | 通过 | 19 个项目 Skill 及方案核心/按需契约通过；工具测试 38 项通过，包括下游 Skill 固定节号防回退测试 |
| 2026-08-01（方案模板精简后） | 独立只读复核 | 通过 | 未发现阻断或重要问题；确认 `R/BR → 代码实施计划 → 验证与验收` 足以指导代码生成 |
| 2026-08-01（方案模板精简后） | `npm run check` | 通过 | AI、文档、契约、类型检查和三端构建通过；Web 99 项、扩展 15 项、后端 195 项测试通过，后端 4 项跳过并有 1 条第三方弃用警告 |
| 2026-08-02（完整章节库调整后） | `check_skills.py` 与 `pytest tests/tooling/test_ai_workflow.py -q` | 通过 | 19 个项目 Skill 和工作流工具测试 38 项通过；覆盖需求描述、状态机、数据模型、代码计划、验证映射、按需删除与固定节号防回退 |
| 2026-08-02（完整章节库调整后） | 独立只读复核 | 通过 | 未发现阻断或重要问题；确认原有 13 章、第 14 章及 `R/BR → 真实文件步骤 → 验证证据` 链路完整 |
| 2026-08-02（完整章节库调整后） | `npm run check` | 通过 | AI、文档、契约、类型检查和三端构建通过；Web 99 项、扩展 15 项、后端 195 项测试通过，后端 4 项跳过并有 1 条第三方弃用警告 |
| 2026-08-02（三项状态修复） | 9 项目标回归测试 | 通过 | 覆盖旧文件迁移、schema v7 遗留修复、旧 schema 与 v7 双文件冲突、Acceptance 非法 `--next`、首次验证失败、命令失败重验、冻结方案漂移重验和验证命令修改代码 |
| 2026-08-02（三项状态修复） | `pytest tests/tooling/test_ai_workflow.py -q` | 通过 | 完整工作流工具测试 43 项通过 |
| 2026-08-02（三项状态修复） | `npm run check:ai` | 通过 | 19 个项目 Skill、5 个变更文件对应的 7 条文档同步规则和 14 组运行时契约通过 |
| 2026-08-02（三项状态修复） | 独立只读审查与修复后复审 | 通过 | 初审发现 v7 双文件冲突和冻结产物漂移重验两个边界；补齐后复审未发现新的状态漏洞 |
| 2026-08-02（三项状态修复） | `npm run check` | 通过 | AI、文档、契约、类型检查和三端构建通过；Web 99 项、扩展 15 项、后端 200 项测试通过，后端 4 项跳过并有 1 条第三方弃用警告 |
| 2026-08-02（五项减法） | `pytest tests/tooling/test_ai_workflow.py -q` | 通过 | 工作流工具测试 45 项通过；新增覆盖无 Issue 正常初始化和恢复提示、分流默认三行与展开条件、短方案一次确认、五项减法关键 Skill 与模板删除变异，以及同内容旧 `brief.md` 的安全清理 |
| 2026-08-02（五项减法） | `npm run check:ai` 与 `git diff --check` | 通过 | 链接、19 个项目 Skill、28 个变更文件对应的 7 条文档同步规则和 14 组运行时契约通过；差异无空白错误 |
| 2026-08-02（五项减法） | `npm run check` | 通过 | 最新代码快照的 AI、文档、契约、类型检查和三端构建通过；Web 99 项、扩展 15 项、后端 202 项测试通过，后端 4 项跳过并有 1 条第三方弃用警告；前端仍有既有大 chunk 警告 |
| 2026-08-02（五项减法） | 最终独立只读审查 | 通过 | 确认五项减法在正式规则、Skill、模板、Spec 工具和防回退测试中一致；三项状态修复未回退，未发现功能性阻断 |
| 2026-08-02（无状态改造） | `pytest tests/tooling/test_ai_workflow.py -q` | 通过 | 工作流工具测试 18 项通过；29 项旧状态机测试删除，新增无状态中心契约、有效 Git 忽略与工作树复制规则，以及 11 类精确删除变异 |
| 2026-08-02（无状态改造） | `skill-creator/scripts/quick_validate.py` | 通过 | 19 个项目 Skill 全部通过结构校验 |
| 2026-08-02（无状态改造） | `npm run check:ai` | 通过 | AI 链接、19 个 Skill、38 个变更文件对应的文档同步规则和 14 组运行时契约通过 |
| 2026-08-02（无状态改造） | `npm run check` | 通过 | 类型检查和三端构建通过；Web 99 项、扩展 15 项、后端 175 项测试通过，后端 4 项跳过并有 1 条既有第三方弃用警告；前端仍有既有大 chunk 警告 |
| 2026-08-02（无状态改造） | 三路最终独立只读复核 | 通过 | 分别核对无状态语义与跨会话恢复、防回退测试与误报边界、完整差异和文档事实；发现的问题修正后复审均无阻断或重要问题 |
| 2026-08-02（项目入口精简） | `npm run check:ai`、`npm run check:docs`、`pytest tests/tooling/test_ai_workflow.py -q` 与 `git diff --check` | 通过 | `AGENTS.md`、`CLAUDE.md` 链接正常，19 个 Skill、18 项工作流工具测试、长期文档、14 组运行时契约和空白检查均通过；未运行应用测试，因为本步只改项目规则和长期文档 |
| 2026-08-04（方案来源收敛） | `npm run check:ai`、`uv run --directory apps/backend pytest tests/tooling/test_ai_workflow.py -q`、7 个修改 Skill 的 `quick_validate.py` 与 `git diff --check` | 通过 | 19 个 Skill、19 项工作流工具测试、长期文档同步、14 组运行时契约、Skill 结构和空白检查均通过；新增防回退覆盖飞书初始输入、`solution.md` 当前权威、方案内修订和禁止下游回写 |
| 2026-08-04（单向交付链） | `npm run check:ai`、`uv run --directory apps/backend pytest tests/tooling/test_ai_workflow.py -q`、9 个修改 Skill 的 `quick_validate.py` 与 `git diff --check` | 通过 | 19 个 Skill、19 项工作流工具测试、长期文档同步、14 组运行时契约、Skill 结构和空白检查均通过；新增防回退覆盖六层边界、普通差异不回流、PR 来源差异说明和一次性 Issue 交付评论 |

## 6. 风险与阻塞

当前没有已知阻塞。

已知风险：

- 本目录内容较多，如果后续不及时更新，会成为新的过期文档；因此每个可验证小步结束后必须更新本页，收口时必须删除整个目录。
- 项目入口临时指针如果在最终 PR 前未删除，会让正式分支长期引用临时计划；删除条件已写入 `README.md` 和阶段 6。
- 当前工作流不建设持续下游同步；飞书不回写，Issue 正文不改写，只在 PR 创建后向主要来源 Issue 发布一条交付评论。未来只有用户明确发起新的初步模块规划时才单独写飞书。
- `.worktreeinclude` 只在创建本地 Codex 托管 worktree 时复制，不能被描述为同步；同一 Spec 若在多个 worktree 分别修改，没有 Git 冲突保护。
- `.specs` 内容保持本地，但 `.worktreeinclude` 本身是跟踪文件；提交规则会把规则推到远端，不会把本地 Spec 内容推到远端。
- 删除状态层后不再自动提供文档漂移检测、旧测试快照复用、唯一下一阶段或活跃任务列表；这是已经接受的取舍，不通过另一份状态文件补回。
- 多份本地 Spec 同时可能匹配且无法从分支、内容和差异确定时，必须询问用户，不能猜测活跃任务。
- 原产品规则要求不使用已经移除的 Google CJK serif 字体族，但 Web 字体栈仍保留 `Noto Serif CJK SC` / `Noto Serif SC` 本地 fallback；本次只把冲突如实迁入长期文档，没有擅自修改代码或替用户解释约束。

## 7. 新会话交接

新会话不要重新从两篇 Downloads 文档开始推断。按以下顺序恢复：

1. 读取 `plans/ai-workflow-redesign/README.md`；
2. 读取本页，确认当前阶段、已完成内容、真实验证和下一步；
3. 读取 `decisions.md`，只采用“已确认”且未被替代的决定；
4. 读取 `workflow-model.md`，了解候选模式、产物和收尾边界；
5. 涉及跨会话恢复、共享和清理时读取 `cross-session-records.md`；
6. 按当前阶段读取 `plan.md`；`phase-1-plan.md` 只在追溯已否决方向时读取；
7. 读取正式 `flow-router`、`solution-generator`、`implementation-execution`、`run-all-tests` 和 `branch-pr-workflow`，核对已经落地的七维判断与五项减法；
8. 需要核实原始问题时再读取 `analysis.md`；
9. 在修改前重新检查 `git status`、当前分支和相关真实代码；
10. 完成一个小步后更新本页，不让聊天记录成为唯一进度来源。
