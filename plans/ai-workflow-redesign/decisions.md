# 关键决定记录

## 1. 使用规则

决定状态只有四种：

- `待确认`：正在讨论，不能作为已经生效的改造要求；
- `已确认`：有明确的用户决定或可核实的项目约束，可以指导后续实施；
- `已替代`：曾经有效，后来被新决定取代，保留用于说明历史。
- `已撤回`：候选从未生效，后来被确认方向取代，保留用于说明为什么不再推进。

每条决定使用稳定编号。修改决定时新增一条并在旧决定中写明替代编号，不直接擦除历史。

## 2. 已确认决定

| 编号 | 状态 | 决定 | 依据 | 影响 |
| --- | --- | --- | --- | --- |
| D-001 | 已确认 | 从最新 `origin/dev` 建立独立改造分支 `refactor/local-ai-workflow-redesign` | 用户要求建立新分支；2026-08-01 对话 | 所有改造先在独立分支实施，不直接修改共享远端分支 |
| D-002 | 已确认 | 使用一个临时、受 Git 管理的文件夹保存分析、计划、决定和进度 | 用户于 2026-08-01 明确同意 | 本目录成为跨会话施工记录，但不是现行规则来源 |
| D-003 | 已确认 | 临时施工记录不能覆盖现有 AI 文档、Skill、脚本、代码或测试表达的当前事实 | 用户接受的处理方案；现有项目文档职责 | 计划中的行为只有真实落地并同步正式规则后才算生效 |
| D-004 | 已确认 | 在项目入口增加一条短的分支临时指针，不把完整计划复制进去 | 用户接受的处理方案 | 新会话能够发现本目录；收口时必须删除指针 |
| D-005 | 已确认 | 改造完成后删除临时文件夹 | 用户于 2026-08-01 明确要求 | PR 最终状态不能依赖本目录解释正式行为 |
| D-006 | 已确认 | 本次工作流改造采用项目允许的自举例外，不使用正在被改造的阶段链约束自身；Git 安全、真实验证和审查底线继续保留 | `.ai/prompts/project.md` 的工作流自举规则 | 可以分阶段直接维护工作流，但不能未验证就宣称完成 |
| D-007 | 已确认 | 第一批改造不自动写 Issue 或飞书 | 现状分析中的风险控制；当前规则也禁止自动回写 Issue | 先在仓库内稳定任务事实、状态和证据，再评估外部同步 |
| D-008 | 已确认 | 完整 CI 继续作为 PR 和共享分支的最终质量兜底 | 当前 `.github/workflows/quality.yml` 与用户接受的总体方向 | 局部验证优化不能替代最终完整检查 |
| D-011 | 已确认 | 阶段 2 继续保留 `solution.md` 作为唯一主 Spec 文档，直接简化现有模板，不新增平级 Change、Design 或第二套状态 | 用户要求精简当前项目的 solution 模板，并强调首要目标是降低复杂度 | 本阶段只改唯一方案文档的核心和按需内容；未来即使改名也不能形成双轨 |
| D-023 | 已确认 | 使用七维判断：准备程度；影响面与相互牵连；规则与状态关系；实现确定程度；验证难度；风险与可回退性；持续性与协作。只有第 2～5 项决定简单、中等或复杂，不计算总分；第 1 项是开工门槛，第 6 项独立决定常规或严格，第 7 项独立决定会话内或持久记录 | 用户于 2026-08-01 明确确认 | 七维在内部完整判断；文档和检查由实际命中的维度触发，对外展示方式由 D-030 进一步收窄 |
| D-025 | 已确认 | 精简后的 `solution.md` 仍保留一张表达本次核心链路的主要流程图，不因模板减法把流程视图全部删除 | 用户在审阅模拟方案后于 2026-08-01 明确提出 | 阶段 2 必须为主流程保留稳定位置；图的类型按任务选择业务流程、交互时序或迁移发布流程，不再额外强制多张重复图 |
| D-026 | 已确认 | 业务流程作为核心内容保留；数据模型按“不读写则省略、只用现有数据则简表、改 schema 或迁移则完整模型与 DDL”分层展开 | 用户接受候选边界并要求按此精简模板 | 未命中的状态、架构、数据、接口和安全章节删除，不保留“不涉及”占位；命中数据库变化时仍保持完整设计深度 |
| D-027 | 已确认 | 精简不能削弱代码生成依据；所有方案必须建立“可观察结果 → 业务规则 → 真实文件与编号步骤 → 验证证据”的映射，下游按语义标题读取而不依赖节号 | 用户要求保证文档能够指导代码生成 | 模板校验保护核心施工契约和按需能力，Acceptance、实现、测试与人工验收改用稳定语义锚点 |
| D-028 | 已确认 | `solution.template.md` 保留原有 13 个章节作为完整章节库，并保留独立的“验证与验收”；实际方案按需求挑选章节。需求描述始终保留，状态机和数据模型命中时优先完整展开 | 用户于 2026-08-02 在对比精简前后模板后明确调整方向 | 不恢复逐章必填和固定节号依赖；模板、Skill 和机器检查同时保护完整章节库、重点条件章节及代码生成映射 |
| D-029 | 已确认 | 用户当前请求、可读取的 Issue、飞书文档和用户指定材料都可以作为需求来源；Issue 存在时完整读取并可选关联，不存在时不阻止分流、Spec、实现或 PR | 用户要求“五项减法都做”；2026-08-02 对话 | `source_issue` 继续保持可选，不新增 schema；无 Issue 使用本地任务标识，不再作为例外或开工门槛 |
| D-030 | 已确认 | 七维继续在内部完整判断，默认对用户只输出路径、一个主导原因和额外检查；仅准备不足、严格风险、需要持久记录或用户主动询问时展开七维依据 | 用户要求“五项减法都做”；2026-08-02 对话 | 降低普通任务的阅读负担，不削弱分流内部检查或严格风险说明 |
| D-031 | 已确认 | 短且无真实待决选择的方案整份只确认一次；长方案或有独立决定时才分段；用户已选直接施工或契约验收时确认方案直接复用，不重复询问；模块规划完成后复用原请求已有继续授权 | 用户要求“五项减法都做”；2026-08-02 对话 | 删除机械重复的确认，保留真实产品、权限、数据、兼容和不可逆选择的确认 |
| D-032 | 已确认 | `implementation_report.md` 只在存在已允许的实际方案偏差、已接受限制或必须跨会话交接的遗留风险或下一步事项时创建，并只保留对应三类增量章节 | 用户要求“五项减法都做”；2026-08-02 对话 | 数据库、跨端、权限、部署、多模块和严格风险本身不再触发报告；不重复正常实现、验证、人工验收或 PR 内容 |
| D-034 | 已确认 | 完整删除 Spec 机器状态层和阶段命令，不迁移旧任务，也不增加 `status.md`、确认总开关、检查点或其他替代状态；方案任务始终以当前 `solution.md` 为中心，AI 根据当前请求、Spec 附件、Git 差异、真实代码和当前验证判断下一步 | 用户于 2026-08-02 明确要求直接删除，并强调减轻强制状态、给 AI 判断余地、仍以 Spec 为中心 | 删除 `flow_guard.py`、`npm run spec`、phase、freeze/refreeze、文档哈希、验证快照、本地质量阶段和完成状态；缺少这些字段不能阻塞继续工作 |
| D-035 | 已确认 | `.specs/*/` 通过 `.worktreeinclude` 复制到新建的本地 Codex 托管工作树，内容继续被 Git 忽略；复制只是一份创建时快照，不承诺持续同步、跨设备或多人同步 | 用户提出可直接加入 `.worktreeinclude`，并于 2026-08-02 确认无旧任务迁移 | 跨会话恢复读取当前 Spec、Git 和代码；多份 Spec 无法唯一判断时询问用户，不建立活跃任务状态表 |
| D-036 | 已确认 | 验证和质量审查仍按改动范围与风险真实执行，但不保存为工作流阶段，也不继承旧会话的缓存结论；需要形成当前结论时重新运行对应命令，准备 PR 时对当前可提交内容运行完整 `npm run check` | D-008、原 D-033 与 D-034 的共同边界 | 删除状态不会降低验证底线，也不建设证据失效图或本地验证缓存 |
| D-037 | 已确认 | 用户可以覆盖自动分流和是否使用 Spec，但不能用流程选择绕过权限、数据、安全、迁移、破坏性操作、Git 授权和真实验证底线 | 项目既有安全边界；本轮目标是减少流程约束，不是降低操作安全 | AI 有路径判断自由，用户也有最终路径选择权；安全检查只按真实命中项保留 |
| D-038 | 已确认 | Spec 是否继续沿用由当前目标和交付边界判断：同一目标继续更新当前 `solution.md`，独立目标建立新的 Spec；不设置关闭状态、归档阶段或自动续改命令 | D-034 的无状态原则 | AI 从方案内容、来源材料和 Git 事实判断；无法区分多份 Spec 时询问用户 |
| D-039 | 已确认 | 飞书只作为方案形成前的初步设计入口；`solution.md` 确认后成为方案任务的当前实施和验收依据。后续设计与飞书有出入时修订原 `solution.md` 并确认受影响决定，不修改飞书 | 用户于 2026-08-04 明确确认 | 取消飞书与 Spec 的并行权威和下游回写；只有用户明确要求开展新的初步模块规划时才使用 `module-planning` 写飞书 |
| D-040 | 已确认 | 整个开发流采用单向层次：飞书和 Issue 正文提供初始输入，Spec 按需收敛可执行方案，代码与测试保存实施事实，PR 说明实际交付和重要差异，来源 Issue 在 PR 创建后只补一条交付评论。各层不要求强一致，也不发送过程同步评论；只有原目标或核心验收失效、形成独立交付边界或模块前提重建时才开启新一轮 | 用户于 2026-08-04 明确要求减少反复修改并保持方向单向 | 普通实现差异不回流；同一目标的重要决定只修订当前 `solution.md`；PR 创建授权默认包含一条来源 Issue 交付评论，但不修改 Issue 正文或状态 |

## 3. 待确认决定

当前没有待确认决定。

## 4. 已替代决定

| 编号 | 状态 | 原决定 | 替代原因 | 替代决定 |
| --- | --- | --- | --- | --- |
| D-024 | 已替代 | 第一小步保留 Spec schema、phase 和冻结行为，只替换七维分流 | 该范围控制已完成历史使命；用户随后决定完整删除机器状态层 | D-034 |
| D-033 | 已替代 | Spec 保存任务范围命令和代码快照，并用本地完成状态区分任务验证与 PR 检查 | 继续保留验证底线，但不再把命令、快照和质量审查维护为状态 | D-036 |
| D-015 | 已替代 | 外部 Issue 或飞书何时可以成为可写决定载体 | 两者都不成为后续决定载体：飞书只作初步设计，Issue 正文只作任务入口；交付时允许向来源 Issue 写一条结果评论，但决定仍收敛在当前请求或 `solution.md` | D-039、D-040 |

## 5. 已撤回候选

| 编号 | 状态 | 原候选 | 撤回原因 | 替代决定 |
| --- | --- | --- | --- | --- |
| D-010 | 已撤回 | 用任务大小、风险和是否交接共同决定哪些直接任务不建持久文件 | 把复杂度、风险和记录需要混在一起；严格风险本身不代表必须持久化 | D-023 |
| D-016 | 已撤回 | 以直接实施、计划实施、调查先行三种工作方式作为目标模型主分类 | 工作方式只是七维判断后的动作，不能作为任务本身的主分类 | D-023 |
| D-019 | 已撤回 | 用“Spec 任务／直接任务”决定跨会话保存方式 | 是否持久记录应由持续性与协作单独判断；具体存放位置继续由 D-009 决定 | D-023 |
| D-020 | 已撤回 | 第一批直接从 Spec 模板、状态和门禁减法开始 | 用户先确认了更基础的七维判断，入口误判应先独立替换，再据此决定 Spec 应展开多少 | D-024 |
| D-009 | 已撤回 | 继续讨论未来可恢复记录放在哪里 | 已确认沿用方案任务的 `.specs/<KEY>/solution.md`，不建设新记录系统 | D-034、D-035 |
| D-013 | 已撤回 | 设计旧 schema v7 的自动迁移 | 用户确认没有旧任务，不需要迁移；状态结构本身被删除 | D-034 |
| D-014 | 已撤回 | 建设可持久化、可按模块失效的验证证据 | 当前选择直接重跑需要的验证，不保存证据缓存或依赖图 | D-036 |
| D-018 | 已撤回 | 把远端检查、审核和发布纳入本地状态模型 | 本地状态模型已删除；远端结果只报告真实外部事实 | D-034、D-036 |
| D-021 | 已撤回 | 继续讨论 Change 是否替代 Spec | 已确认不建设 Change；方案任务以 `solution.md` 为中心 | D-034 |
| D-022 | 已撤回 | 继续讨论是否只覆盖本机 Codex 托管工作树 | 已确认采用 `.worktreeinclude` 的本机创建时快照，并接受其边界 | D-035 |
| D-012 | 已撤回 | 继续讨论用户覆盖自动判断的边界 | 正式规则已经明确区分路径自由和不可绕过的安全底线 | D-037 |
| D-017 | 已撤回 | 为同一个工作对象设计固定续改期限 | 无状态模型不维护任务生命周期；按目标是否相同做语义判断 | D-038 |
