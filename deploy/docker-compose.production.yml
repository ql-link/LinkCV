services:
  linkresume-pi:
    image: linkresume-pi:${PI_TAG:?PI_TAG is required}
    container_name: linkresume-pi
    restart: always
    env_file:
      - ${LINKRESUME_ENV_FILE:-../.env.production}
      - ${LINKRESUME_SECRET_ENV_FILE:-../.env.production.local}
    environment:
      APP_ENV: production
      LINKRESUME_BASE_URL: http://linkresume:8000
      PI_SERVICE_HOST: 0.0.0.0
      PI_SERVICE_PORT: 8010
      TZ: Asia/Shanghai
    networks:
      - linkresume-runtime
    labels:
      com.linkresume.environment: production
      com.linkresume.role: pi-agent
    depends_on:
      linkresume:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:8010/health').then(r=>{if(!r.ok)process.exit(1)})"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  linkresume:
    image: linkresume:${TAG:-latest}
    container_name: linkresume
    restart: always
    env_file:
      - ${LINKRESUME_ENV_FILE:-../.env.production}
      - ${LINKRESUME_SECRET_ENV_FILE:-../.env.production.local}
    environment:
      APP_ENV: production
      BACKEND_HOST: 0.0.0.0
      BACKEND_PORT: 8000
      WEB_DIST_DIR: /app/web
      TZ: Asia/Shanghai
    ports:
      - "${LINKRESUME_HTTP_PORT:-4174}:8000"
    volumes:
      - linkresume-production-logs:/app/logs
    networks:
      - linkresume-runtime
    labels:
      com.linkresume.environment: production
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/health')",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  promtail:
    image: grafana/promtail:2.9.8
    container_name: linkresume-promtail
    restart: always
    command:
      - -config.file=/etc/promtail/promtail-config.yml
      - -config.expand-env=true
    env_file:
      - ${LINKRESUME_ENV_FILE:-../.env.production}
    volumes:
      - ./observability/promtail-config.yml:/etc/promtail/promtail-config.yml:ro
      - linkresume-production-logs:/var/log/linkresume:ro
      - linkresume-production-promtail-positions:/tmp
    networks:
      - linkresume-runtime
    depends_on:
      linkresume:
        condition: service_started
      linkresume-worker:
        condition: service_started

  linkresume-worker:
    image: linkresume:${TAG:-latest}
    container_name: linkresume-worker
    command: ["python", "-m", "linkresume.workers"]
    restart: always
    env_file:
      - ${LINKRESUME_ENV_FILE:-../.env.production}
      - ${LINKRESUME_SECRET_ENV_FILE:-../.env.production.local}
    environment:
      APP_ENV: production
      LOG_DIRECTORY: /app/logs/worker
      TZ: Asia/Shanghai
    volumes:
      - linkresume-production-logs:/app/logs
    networks:
      - linkresume-runtime
    labels:
      com.linkresume.environment: production
      com.linkresume.role: resume-import-worker

networks:
  linkresume-runtime:
    external: true
    name: ${LINKRESUME_DOCKER_NETWORK:-tolink-app-net}

volumes:
  linkresume-production-logs:
    name: linkresume-production-logs
  linkresume-production-promtail-positions:
    name: linkresume-production-promtail-positions
