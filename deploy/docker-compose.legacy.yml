# Transitional production topology for the legacy Express application.
# New local development uses deploy/docker-compose.yml instead.

services:
  linkcv:
    image: linkcv:${TAG:-latest}
    container_name: linkcv
    restart: always
    env_file:
      - ${LINKCV_ENV_FILE:-../.env}
    environment:
      TZ: Asia/Shanghai
      NODE_ENV: production
      API_PORT: 4174
      DATA_DIR: /app/data
    ports:
      - "4174:4174"
    volumes:
      - /opt/tolink/LinkCV/data:/app/data
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:4174/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
