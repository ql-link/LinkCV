# 简历与工作台功能

AI 提案确认与自动保存共用写入队列，保存失败不继续确认。确认期间目标编辑器和样式设置暂停编辑，成功后从服务端响应同步当前正文；该操作不新增可恢复历史版本。创建、导入、复制和翻译同样只产生当前简历，不再创建历史版本。

## 功能范围

简历功能覆盖模板创建、文件导入、编辑与自动保存、独立复制、模板切换、资源、分享和 PDF 输出。浏览器维护编辑中的交互状态，服务端当前简历是持久化权威数据。

具体 schema、稳定错误和并发冲突见 [HTTP 接口契约](../api/http-contracts.md)，编辑器视觉与交互基线见 [Web 架构](../internals/web.md)。

## 用户入口

- `/resumes`：简历列表、创建、导入、重命名、复制为新简历、删除和分享。
- `/resumes/new`：从启用模板创建简历。
- `/resumes/:resumeId/edit`：正文编辑、页面设置、模板切换、版本记录、AI 侧栏和 PDF 导出；生产构建暂不显示打开 AI 侧栏的悬浮入口。
- `/assistant` 内嵌工作台：从助手页右上角“我的简历”选择已有简历后，在对话右侧复用正文编辑与自动保存，但不渲染整条顶部工具栏，也不保留其高度占位。关闭、撤销/重做、标题、设置、模板和更多操作栏仅在独立简历编辑页展示；正文选区浮层不受影响。发送请求前先保存脏草稿，并把当前简历作为该条消息的上下文，选中文字时再附带稳定节点、范围、原文和摘要；会话本身不绑定简历。应用当前内嵌简历的 Agent 提案时再次先保存本地编辑，确认成功后重新读取服务端快照并无副作用地替换 Tiptap 文档，使修改即时显示；其他简历提案不替换当前编辑器。
- `/templates`：模板浏览与预览，页头右侧提供“筛选”按钮，卡片紧接页头展示且不再显示模板数量摘要；点击“筛选”展开下拉层，可按视觉风格（简约、经典、现代、创意）和适用场景（实习、校招、社招）分别多选，点选后立即应用；同组匹配任一标签，跨组同时满足。卡片展示已标注标签，没有结果时可清除筛选。模板按管理员设置的展示顺序升序排列，相同排序值按模板 ID 排列。编辑器切换模板侧栏复用同一套分类筛选和展示顺序。
- 默认目录在迁移 `0081` 后包含 85 套启用模板，已下架经典单栏、现代双栏、紧凑技术型三套废弃设计。其中 Atlas 系列提供 37 套排版，Studio 系列提供六套卡片、双色分区、肖像、胶囊与时间轴样式。新增 Open 系列六套 MIT 授权排版，来源见[模板授权记录](../internals/resume-template-sources.md)。Original 系列包含朱砂编辑笺、横向分庭、墨绿档案签、横轴履历、暖灰书页、非对称索引、错位页眉、折页边注、悬挂章节九套经图像设计确认的原创模板。Career 系列新增青墨财务档案、橙标精益履历、留白师范履历、细线护理履历与蓝线传播履历，分别覆盖财务、制造业社招，教师、护理校招和市场营销实习；校招、实习突出教育与实践，并标明预计毕业和资格状态。各模板使用虚构示例内容；创建时保存模板快照，后续目录更新不覆盖已有简历。Featured 系列包含蓝幕校招、浅蓝社招、清蓝实习、钴蓝右栏、蓝色拼版、蓝线财务和蓝带人事，并增加卡片虚线、卡片分栏、经典商务与活力四套版式；已有的分层胶囊章节、节点时间轴不重复新增。
- `/share/:token`：受可见性与过期时间约束的只读分享页。
- 管理端 `/admin/templates`：严格模板包导入、列表缩略图和完整预览、启停及展示顺序调整；列表缩略图与用户模板库复用同一只读渲染器，结构无效模板显示占位。排序值越小越靠前，相同值按模板 ID 排列。迁移 `0087` 仅在没有人工排序值时按原有 ID 顺序将存量模板初始化为 10、20、30……；新导入模板取当前最大排序值加 10（上限 1000000）。“模板分类”视图供管理员逐套查看启用且有效的模板，可为一套模板分别多选视觉风格与实习、校招、社招适用场景，或将风格标记为待讨论。分类通过管理员接口写入模板表并同步到用户模板库，也可导出按模板 key 标识的 JSON。迁移 `0085` 为当时的 85 套启用模板写入初版标签；之后的调整以数据库为准，新导入模板默认未分类。

简历列表、重命名与导入弹窗、独立新建/导入页、模板创建弹窗和编辑工作台的操作异常统一通过页面根层在视口上方居中提示，不受业务弹窗或卡片定位影响，并在 3 秒后自动消失；编辑器中的字段校验仍保留在对应表单附近。

## 代码地图

| 层级 | 入口 | 职责 |
| --- | --- | --- |
| 领域契约 | `src/linkresume/domain/resume/` | `CanonicalResumeDocument`、`TemplateDefinition`、`ResumePresentation`、`LayoutPlan`、`SourceGraph` 与确定性组合 |
| 应用服务 | `application/resumes/` | 创建、保存、复制、模板切换和分享事务 |
| HTTP/ORM | `modules/resumes/` | 简历、版本、模板、导入、资源、分享和 PDF 路由与模型 |
| 导入 | `workers/resume_import_worker.py`、`services/resume_import_service.py` | 异步解析、结构化、规范化和结果事务 |
| Web 状态 | `store/resumeStore.ts`、`api/resumeContract.ts` | 编辑状态与 TypeScript 领域契约 |
| Web 功能 | `features/home/`、`workbench/`、`preview/`、`templates/`、`share/` | 列表、编辑、打印、模板与分享界面 |

## 核心对象与规则

- `resumes` 保存当前 canonical 内容、呈现、模板、分享状态和 `lock_version`；保存和模板切换必须通过乐观锁。正文只保存模板无关的 identity、语义章节、块、章节内 row 和来源引用，不能保存 sidebar/main、CSS 或分页投影。
- `resume_versions` 只保留切换前存量内容供读取和复制取回；旧创建、重命名、删除和恢复接口返回 410，不在日常工作台展示。
- `resume_templates` 保存 `TemplateDefinition` 与默认 canonical 内容；普通用户只消费启用且结构有效的模板。模板拥有区域、插槽、列宽和头像显示策略，`LayoutPlan` 是后端编译的只读投影结果。
- Web 默认简历中的图片占位标签使用随应用发布的霞鹜文楷；它只负责示例占位图呈现，不覆盖用户保存的简历字体设置。
- canonical 正文把白名单内的简历图标保存为结构化 `InlineIcon`，章节标题使用独立 `title_icon`；`:icon[Name]:` 只作为 Markdown、Agent 和旧数据兼容边界的序列化形式，不能作为预览中可见的普通正文。未知或不完整标记继续按原文字保留，避免静默改写用户内容。
- 简历图片在进入当前快照前统一校验 PNG/JPEG 格式、单图 10 MiB 上限和快照引用总量 10 MiB；不满足条件时保存、模板切换、复制和 Agent 提案确认均明确失败，避免延迟到 PDF 导出阶段才暴露问题。
- 用户确认整篇翻译提案时会创建一份独立可编辑简历，原简历不变。账户级图片继续共享；源简历专属图片复制到新简历命名空间并改写引用，复制或事务失败时不保留半成品简历。
- canonical 列表保留用户创建的空项，空项使用 `runs: []` 表达，不写入空格或占位文本；列表块本身仍至少包含一个列表项。
- Web 编辑器在可容纳列表的普通段落行首输入 `-`、`*` 或 `+` 加空格时创建无序列表，保留隐藏定位锚点，并合并相邻的无序列表；正文中的减号、负数和年份范围保持普通文本。数字与无序列表回车续项后，空项按退格或再次回车会退出一层列表结构，保留当前空白段落和光标；顶层退出后再按退格才删除空行、返回上一行，嵌套列表逐层退出。
- 标题行首按回车在标题上方插入空段落而非拆出空标题，光标留在标题行首；文本块行首按退格时若上一块是空行则删除空行、当前行类型不变，若是标题且上一块有内容则并入上一文本块；空行内按 Delete 删除该空行并把光标落到下一行行首。行首判定把隐藏定位锚点前后的停靠位都算作行首。编辑器内中文输入法组合期间，命令面板与分栏栏数菜单不响应按键，分页测量也推迟到组合结束后再派发。姓名行（一级标题）之后首个有内容的段落是 headline 行，编辑器按文档结构为其标记 `resume-identity-headline` 并套用与 `h1 + p` 相同的主题样式，中间插入空行不再破坏排版；非编辑器渲染（预览、分享、PDF）仍按 `h1 + p` 相邻关系取样式。
- `document_parse_tasks` 保存简历导入和资料集共用的上传/解析状态，`file_format` 约束覆盖文档四类和音视频七类（媒体任务不使用解析队列，直接落终态）。简历导入在受理时同时冻结 `selected_template_id` 与规范化的 `selected_template_style_json`（完整 `TemplateDefinition`），并把确定性的 `SourceGraph` 保存到私有对象；Worker 只使用任务快照，因此模板之后更新或停用不会改变已受理任务的版式。资料集任务额外使用 `queued`、派发时间和尝试版本完成 MQ 恢复。PDF/DOCX 由 LinkParse 转换文字并可附带有界布局提示，Markdown 在 Worker 本地转换。PDF 始终使用 `include_images=false`：文字与图片混排的 PDF 继续解析文字，但证件照、Logo 和其他源图片不会进入导入结果，模板头像保持为空。
- 分享实时读取当前已保存草稿，不另存内容快照；PDF 同样使用当前已保存快照生成且不持久化成品。

## 依赖边界

该功能依赖身份、MySQL、MinIO、Redis、消息队列、LinkParse 和统一 LLM。求职进程保存独立的投递附件；AI 助手只能先创建提案，用户确认后才写入简历。删除简历时清理目标为该简历的待确认提案，但保留独立对话历史，且不能越过其他领域的引用约束。

## 关键流程

### 创建与编辑

命令面板会用强调色标出光标所在行真实的块类型（正文、标题 1–3、列表、左右分栏）；左右分栏支持两侧独立输入，光标位于分栏行内时再点一次“左右分栏”即可取消分栏、合并回普通正文，也能用退格删除已经清空的分栏行；右键分栏行可切换为 3 栏或 4 栏；3/4 栏默认等分；光标位于分栏行内时，2/3/4 栏均可拖动栏间分隔线调整宽度，双击分隔线恢复等分，改栏数后重新等分。2 栏沿用 30%–80% 的左右比例，3/4 栏仅调整相邻两栏且各栏至少保留 10%；调整结果保留在正文快照中，保存、模板切换和导出继续沿用既有排版规则。光标落在分栏行内时，还没有可见内容的栏显示“左侧内容…”“右侧内容（日期、地点等）…”这类编辑提示，3/4 栏的行只在首栏提示“每栏独立输入…”；该栏一旦有文字、图标或图片就不再提示，提示本身只用于编辑，不进入保存内容、分页和导出。分栏与其他区块中的图片、图标等可选中叶子节点支持鼠标点选为节点选中，便于剪切复制；按住从这类节点拖动时选区锚点固定在节点自身边界，行内原子可按拖动方向扩展覆盖相邻文字，块级原子拖动时保持节点选中、不扩展进周边文字，从文字拖入这类节点且端点落在其上时选区恰好覆盖该节点。选区覆盖某个分栏/布局节点的全部内容时，复制与剪切携带整个结构节点：剪切会连结构一起移除、不在原地留空壳，粘贴到别处能还原分栏排版；编辑器内部剪贴板（带 data-pm-slice 标记）粘贴时保留节点结构，外部来源 HTML 仍拍平为纯段落。栏数固定的行（左右分栏行、三栏/四栏等分行、页头三行与四行）内按回车不在行内分段，而是统一退出行并落到紧随其后的空白段落，行后已有空白行时不重复插入；退格可删除这个退出产生的空白行并回到行末格。光标或选区落在分栏行、页头三行/四行内时，该行为浅蓝激活外框；选区完整覆盖某个布局节点（分栏行、页头行或整组分栏）的全部内容时，外框落在该节点本身，提示即将被整体剪切复制的结构。栏容器与普通段落不显示常态外框，避免正文区域整片染色。

可编辑画布使用浏览器原生细光标，并沿用所在文字的颜色：浅色区域为深色光标，深色区域为浅色光标；不再插入额外光标装饰或遮挡文字。中文输入法组合输入也使用同一原生光标。

头像默认按一寸照约 25×35mm 的竖向比例展示（画布宽约 94 CSS px），普通模板保持方角或轻微圆角；明确使用圆形或拱形肖像的模板保留 1:1 裁切。选中头像只显示细描边，不改变照片形状；按住 ⌘/Ctrl 滚轮调整宽度时同步保持当前模板的纵横比例，已手动调整的尺寸在模板投影中保留。打印和只读预览采用同一尺寸规则。

行内图片选中时叠加轻量遮罩和描边，并在图片上方显示固定锚定的尺寸工具条。工具条只展示当前宽、高数值及各自上下排列的增大/减小箭头，每次调整 1px，结果限制在 16–240px；不提供尺寸文字输入或替代文字重命名。尺寸变化后图片保持选中且工具条位置不随宽高移动；再次单击普通正文会解除图片节点选区，并把文本光标放到实际点击位置。

选中文字后，浮动工具栏按粗体、删除线、斜体、下划线、链接、字体、本行类型、对齐方式排列，块级的两项与前面的内联格式之间用竖线分隔，下拉统一向下展开。「链接」为选区填写网址，选中已有链接时可取消，并沿用简历规则拒绝把邮箱或 `mailto:` 设为链接；「本行类型」在正文与标题 1–3 之间切换当前块，二级标题是章节锚点，改成其他层级会让该块失去章节语义；「对齐方式」设置当前块的左 / 中 / 右对齐，并随正文保存，刷新、模板切换、只读预览、分享与 PDF 都使用同一对齐；联系方式行整行共用一个取值，行内首个非空联系方式决定方向。「字体」弹层把字号、字体颜色和背景颜色收在一处：字号沿用 6–48 pt、0.5 pt 步进的数值控制，未覆盖时显示模板或全局排版的实际数值，混合选区显示“混合”并以首字字号为基准统一调整；字体颜色以文字颜色本身渲染色板，背景颜色为两行色块，两者都用斜杠表示“取消”，并可一键恢复默认。列表、缩进和「AI 修改」不出现在选中弹窗，撤销 / 重做仍是编辑器顶部左侧的常驻按钮（无可撤销内容时置灰）。浮动工具栏随画布缩小同步变小，画布放大到 100% 以上时保持基准尺寸，不再跟着放大。支持单字、跨段落、姓名、章节标题、联系方式、经历字段、列表及分栏正文；局部字号与文字对齐随当前快照和正式版本保存，并用于模板切换、只读预览、分享与 PDF。

1. 创建时校验用户额度、名称和启用模板，在同一事务写入当前简历和 initial 版本。
2. 编辑器根据当前 `LayoutPlan` 把 canonical 内容投影为可编辑视图。页面设置面板只提供页面排列、模块顺序、上下与左右边距和正文排版；模块顺序只改变章节先后，不改变章节语义，语义仍随 canonical 内容保存，也不在该面板中手动调整。选中文字后的格式浮层锚定在选区最后一个字的右下角，浮层尺寸与间距跟随当前画布缩放，并在滚动、缩放或视口变化后重新定位。正文和页面设置先同步更新前端 Store 并立即作用于画布，边距预览不等待数据库保存完成；未写回数据库的修改按用户和简历隔离保存为浏览器本地草稿，在重新载入相同 `lock_version` 时恢复。数据库自动保存按 10 秒固定节拍检查脏状态，先反投影为 canonical 文档再串行发送 `base_lock_version`，不因每个字符或单次页面设置调整单独请求；成功后接续服务端新锁版本，工作台页头不再提供手动保存按钮。
3. 模板切换把当前标题、canonical 内容、目标模板和锁版本一次提交。服务端校验切换前后正文摘要一致，只替换模板身份和 presentation，并在同一事务递增一次锁；头像仍属于 identity，模板只决定是否和在哪里展示。

### 导入与复制

1. 导入弹窗通过带可访问名称的原生文件输入支持点击选择和拖放，选择后展示候选文件并由用户确认名称；提交后在校验启用模板的同一受理事务中写入原件、导入任务、模板 ID 与完整模板定义快照，再发布消息，队列失败不会伪装成已受理。
2. Worker 根据格式选择本地 Markdown 转换或 LinkParse，把解析块与可用坐标统一成稳定 `source_id` 的 `SourceGraph`。LLM 只返回可选的稀疏语义增强，未标注源块由确定性组合器按来源顺序保留；结构化调用未配置、超时、供应商失败、响应无效或标注校验失败时记录脱敏 warning 并返回匹配该来源图的空标注，因此不会因增强不可用而让整份导入失败，也不会生成“未分类内容”。
3. 组合器把 canonical 内容注入任务冻结的模板定义，编译并校验 `LayoutPlan` 后，在用户行锁内分配导入标题：文件名 stem 已被占用时依次追加 `1`、`2`，再在同一事务创建当前简历并完成任务；任务快照缺失、模板身份不一致、来源图或布局任一不一致时不创建半成品。
4. 复制当前简历需要内部锁标记及请求 UUID；重试返回同一份副本。副本有独立标题、正文、样式和私有图片，不继承分享链接或导入任务。存量历史内容只能复制为新简历，不能恢复覆盖当前内容。

### 分享与输出

分享 token 定位简历主记录中的当前已保存草稿，并执行可见性、过期和所有者判断；自动保存成功后，公开页无需创建正式版本即可读取新内容，浏览器中尚未保存成功的本地编辑不会公开。公开响应把当前草稿引用的本人私有图片转换为受控 data URI，分享页因此不需要匿名访问私有资源路由；`private` 链接仍只允许所有者登录访问。分享页把当前草稿按 A4 分页并纵向排列，保留模板样式，不回写草稿中保存的智能一页设置；加载、正常内容和失效状态都固定使用浅色主题，不跟随访问者的系统深色模式切换。分享页不提供浏览器打印入口；分享者允许下载时显示“下载 PDF”，下载沿用编辑器的服务端 Node/Chromium 渲染链路，按 A4 分页生成纯简历文档，不包含分享页头部和按钮。关闭下载后公开页隐藏入口，PDF 接口也对包括分享者本人在内的所有访问者拒绝下载，不能通过直接请求绕过。分享面板创建链接时直接使用当前选择，链接默认明文并可一键复制；已有链接的可见性、有效期和下载权限修改后立即保存，界面始终显示真实到期时间，仅重新生成和删除保留二次确认。面板根据管理接口状态判断可用性，不额外下载完整公开分享 payload。页面设置中的霞鹜文楷选项在编辑器预览使用随应用发布的 Medium 字重、在 PDF 导出使用 Regular 字重，用户选择的字体保存在当前模板的 `template_scoped.font_family` 覆盖中，缺少覆盖的旧简历继续使用模板默认字体；简历图片只接受 PNG/JPEG，上传、公开分享与 PDF 导出共用 10 MiB 单图上限，当前快照引用图片总量也限制为 10 MiB，并在保存、模板切换和复制前校验。编辑器 PDF 先完成当前保存，分享 PDF 直接读取当前已保存草稿；两者都由后端读取受控图片、调用一次性 Node/Chromium 渲染器返回文件，打印 DOM 与浏览器只读预览共同消费同一快照和样式事实源，其中霞鹜文楷的字重按上述规则区分。

移动端分享页把固定 210mm 的 A4 DOM 按滚动容器实际内容宽度整体缩放（CSS `zoom`，不支持时回退 `transform: scale` 并以缩放后高度占位）；`text-size-adjust` 抑制浏览器文字放大，微信 webview 再通过官方 `WeixinJSBridge` 的 `setFontSizeCallback` 把字号恢复标准档，避免字体放大撑破固定纸面造成重排变形。屏幕缩放只服务小屏预览，打印媒体下解除缩放仍输出原始 A4 文字版。响应通过 `assets` 字段内嵌渲染图片（data URI），匿名访问者可直接查看简历内图片（见 HTTP 契约）。

### 完整度检查

评分前移除编辑器锚点及受控的局部字号、颜色、高亮和下划线标记，拼接连续的可见文字；改变单字或段落样式不会改变姓名、联系方式、章节识别和完整度得分。

工作台对当前实时 Markdown 运行确定性规则，覆盖姓名、电话、邮箱、职业定位、经历、教育、技能和基本结构，不依赖 API、LLM 或外部评分服务。技能检查只读取技能类章节正文，不从教育、工作或项目正文借用技术词加分；标题、空内容和占位文字不计分，条目数量与工具、熟悉程度或应用场景等具体程度分别计分。

1. `setEditorContent` 每次编辑都同步刷新当前 Markdown，评分无需等待自动保存或手动保存。
2. 顶部“完整度”入口展示 0–100 分；点击后打开“简历检查”抽屉，逐项显示状态、得分、问题和建议。低于 40 分显示红色，40–80 分显示黄色，高于 80 分显示蓝色。
3. 系统示例身份按未填写处理并把总分封顶为 20，残留示例学校、公司或项目把总分封顶为 60。结果不评价整体表达质量或岗位匹配度，也不写入 Store 持久状态、API、数据库或版本。
4. 检查失败或低分不阻止编辑、保存、创建版本和导出；恢复版本或切换简历后根据当前正文重新计算。

## 权限、并发与失败边界

- 所有简历、版本和资源操作同时校验 `resume_id + user_id`，不能仅凭资源路径授权。
- 乐观锁冲突、保存失败、模板无效、渲染失败或 Agent 提案冲突都保留客户端当前内容，不触发后续下载或部分写入。
- 私有图片只允许受控对象键和格式；PDF 渲染不联网读取正文资源，也不持久化输出。
- 删除简历、版本、模板或资源时必须先核对求职进程、分享、Agent 和对象存储影响。

## 修改联动与验证

修改快照结构时需同步 Python/TypeScript 契约、迁移、模板包、编辑器、预览、分享、PDF 和小程序预览；修改导入需同步 Worker、MQ、LinkParse、LLM 和开发/部署说明。主要测试入口包括后端 `test_resume_lifecycle.py`、`test_resume_imports.py`、`test_resume_share.py`、`test_resume_pdf.py`、模板管理与迁移测试，前端 `ResumeWorkbench`、`resumeStore`、`resumeContract`、打印、模板和分享测试，以及 Worker/导入服务单元测试。视觉模板目录测试直接读取 `0070–0076`、`0078–0081` 的 SQL 模板快照，并按 Atlas、Studio、Open、Original、Career、Featured 的主题清单核对模板身份与打印渲染；迁移重编号时必须同步这些测试输入。
