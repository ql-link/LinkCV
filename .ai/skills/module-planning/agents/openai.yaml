interface:
  display_name: "LinkCV 模块规划"
  short_description: "从当前请求与可用材料收敛模块方案，写入飞书并按已有授权继续"
  default_prompt: "使用 $module-planning 从当前请求、可用 Issue 和飞书材料充分讨论这个模块，确认完整草稿后写入并读回飞书；若原请求已授权后续开发，读回一致后继续到相应路径，否则停止并交接。"
