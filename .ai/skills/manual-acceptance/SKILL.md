---
name: manual-acceptance
description: 为 LinkCV 的 L2/L3 任务生成、修订和记录人工端到端验收清单。适用于实现和自动化测试完成后，仍需通过真实浏览器核实跨 Web、FastAPI、临时 Express、Cookie、上传下载、PDF 或视觉行为，或者用户明确要求人工验收清单；产物固定写入对应任务的 Spec 目录，不编写自动化测试，也不得在没有人工执行证据时标记通过。
---

# 人工端到端验收

## 1. 职责与边界

把已冻结的验收规则转换为人可以按步骤执行、记录和复核的端到端清单，并将实际结果保存到当前任务的本地 Spec 快照。

本技能只处理自动化测试尚未覆盖的行为，例如：

- 浏览器中的完整用户流程和页面状态切换；
- Web、FastAPI 与临时 Express 之间的真实路由协作；
- Cookie、登录态、上传、下载、打印和 PDF 导出；
- 字体、排版、响应式布局以及必须肉眼确认的视觉结果；
- 需要真实本地服务或基础设施协作的关键流程。

以下情况不要使用：

- 编写组件、单元或后端集成测试，转 `test-authoring`；
- 运行自动化测试并判断仓库是否通过，转 `run-all-tests`；
- 纯后端函数、局部逻辑已经被自动化测试充分覆盖且没有人工观察面；
- 用人工验收替代本应存在的自动化测试。

## 2. 产物位置与生命周期

固定写入：

```text
.specs/<KEY>/manual_acceptance.md
```

生成时读取 [manual_acceptance.template.md](manual_acceptance.template.md)，替换全部占位内容。该文件是任务级执行记录，不放入 `docs/`；`.specs/<KEY>/` 默认不提交，因此 PR 中只摘要结论和失败项。只有用户明确授权时，才能把结论写回 Multica 或其他外部 Issue。

清单可以在实现前预先生成，但只有在目标实现和自动化验证稳定后才执行。结果变化时直接更新原文件，不另建多份“最终版”。

## 3. 必读材料

1. `.specs/<KEY>/state.yaml`；
2. 冻结的 `brief.md`、`acceptance.feature`，L3 还要读取 `technical_design.md`；
3. 当前实现、自动化测试结果和未覆盖区域；
4. 与流程相关的 Web、FastAPI、Express、配置和部署事实；
5. 本技能所属模板。

不要根据聊天印象重新发明验收规则。发现清单需要新的业务结果时，返回 `acceptance-generator` 修订并重新冻结。

## 4. 生成清单

1. 从 `acceptance.feature` 提取需要真实浏览器或真实服务协作的场景。
2. 排除已经由稳定自动化测试完整覆盖、且没有视觉或跨端观察面的场景。
3. 为每项保留验收场景名称，补充环境、测试数据、前置条件、编号、操作步骤和具体预期。
4. 一条步骤只包含一个可执行动作；一条预期只描述一个可以观察或核对的结果。
5. 覆盖适用的主流程、权限、失败、边界、兼容和回归行为，不为增加数量复制相同流程。
6. 测试数据使用虚构信息，默认姓名为“张三”；不得把真实账号、简历、联系方式、Cookie 或密钥写入文件。

## 5. 记录执行结果

- 状态只允许：`未执行`、`通过`、`失败`、`阻塞`、`不适用`。
- AI 可以启动本地服务、准备虚构数据和指导执行，但不能把“计划执行”写成“已经通过”。
- 只有执行人明确确认，或存在本次真实浏览器操作、截图、日志等证据时，才能标记 `通过`。
- `失败` 必须记录实际结果、复现条件和关联缺陷；`阻塞` 必须记录环境或依赖原因。
- 截图和日志记录路径或链接即可；不要把大体积二进制产物放入 `.specs`。
- 任一必要项为 `未执行`、`失败` 或 `阻塞` 时，人工验收结论不得写“通过”。

## 6. 与验证状态衔接

自动化验证已通过且所有必要人工项均通过后，才记录完成证据：

```bash
npm run spec -- verify <KEY> \
  --evidence "npm run check" \
  --evidence "人工验收：.specs/<KEY>/manual_acceptance.md（通过）"
```

如果任务不需要人工验收，由 `run-all-tests` 直接记录自动化证据。如果需要但尚未完成，`run-all-tests` 只能报告自动化结果，不能提前把 Spec 标记为 `done`。

## 7. 完成输出

最终报告至少说明：

- 文件位置和覆盖的验收场景；
- 执行环境、分支或提交、执行人和日期；
- 通过、失败、阻塞、未执行和不适用数量；
- 失败或阻塞项及下一步；
- 自动化验证证据和人工验收是否足以进入质量审查。
